export type BusinessPost = {
  title: string;
  date: string;
  desc?: string;
  img: string;
};

export type ForecastPost = {
  title: string;
  date: string;
  desc?: string;
  img: string;
};

export type FormValues = {
  fullName: string;
  email: string;
  serviceType: string;
  description: string;
};
